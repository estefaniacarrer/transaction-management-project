CREATE TABLE cliente (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   nome VARCHAR(255),
   cpf VARCHAR(255),
   CONSTRAINT pk_cliente PRIMARY KEY (id)
);

CREATE TABLE empresa (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   nome VARCHAR(255),
   cnpj VARCHAR(255),
   saldo DECIMAL,
   CONSTRAINT pk_empresa PRIMARY KEY (id)
);

CREATE TABLE transacao (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   tipo VARCHAR(255),
   valor DECIMAL,
   data TIMESTAMP,
   cliente_id BIGINT,
   empresa_id BIGINT,
   CONSTRAINT pk_transacao PRIMARY KEY (id)
);

ALTER TABLE transacao ADD CONSTRAINT FK_TRANSACAO_ON_CLIENTE FOREIGN KEY (cliente_id) REFERENCES cliente (id);

ALTER TABLE transacao ADD CONSTRAINT FK_TRANSACAO_ON_EMPRESA FOREIGN KEY (empresa_id) REFERENCES empresa (id);